# 🌸 春晓画室 — 网页开发文档

> 最后更新：2026-06-19（登录页重做 + 多孩子子系统 + 家长本地密码认证 + 验证码Web函数部署）

## 项目概述

| 项目 | 详情 |
|------|------|
| **网站名称** | 春晓画室 |
| **GitHub** | https://github.com/Dongfang-jie/chun-xiao |
| **线上地址** | https://dongfang-jie.github.io/chun-xiao/ |
| **用途** | 线下美术/书法兴趣班的官方网站 |
| **目标用户** | 公开访客、家长（登录查看）、老师（登录管理） |
| **部署方式** | GitHub Pages（前端）+ 腾讯云 CloudBase（后端） |

---

## 技术栈

| 技术 | 用途 |
|------|------|
| HTML5 / CSS3 | 页面结构 + 视觉样式（Flexbox、响应式、深色模式） |
| JavaScript (ES5) | 交互逻辑，不编译，浏览器直接运行 |
| CloudBase Web SDK 2.21.0 | 数据库 + 认证 + 云函数调用（CDN 加载） |
| localStorage / sessionStorage | 本地数据缓存 + 登录态持久化 |
| CloudBase 云函数 (Node.js 18.15) | dbProxy（数据库代理）、verify-code（邮箱验证码） |
| QQ 邮箱 SMTP | 发送验证码邮件（nodemailer） |
| ServerChan | 预约表单微信通知 |
| Git + GitHub Pages | 版本控制 + 自动部署 |

---

## 文件结构

```
春晓画室/
├── index.html                       # 首页
├── gallery.html                     # 作品展示页
├── courses.html                     # 课程介绍页
├── contact.html                     # 联系/预约页
├── login.html                       # 登录/注册/忘记密码
├── teacher-dashboard.html           # 老师管理端
├── parent-dashboard.html            # 家长端
├── cloudbaserc.json                 # CloudBase 云函数配置
├── CLAUDE.md                        # Claude Code 项目指令
├── css/
│   ├── style.css                    # 全局样式（公开页）
│   ├── login.css                    # 登录页样式
│   └── dashboard.css                # 仪表盘样式（老师+家长共用）
├── js/
│   ├── app/
│   │   ├── config.js                # CloudBase SDK 初始化
│   │   ├── auth.js                  # 认证模块（教师硬编码 + 家长CloudBase）
│   │   ├── data.js                  # 数据层（localStorage + CloudBase 直接同步）
│   │   ├── storage.js               # CloudBase 云存储工具（upload/getUrls/remove）
│   │   ├── sync-status.js           # 同步状态气泡（右下角实时提示）
│   │   ├── ui.js                    # 灯箱 / 深色模式 / 回顶 / 导航
│   │   └── gallery.js               # 公开画廊页渲染
│   ├── dashboard/
│   │   ├── core.js                  # 登录检查 / 侧边栏 / 页面入口
│   │   ├── parent.js                # 家长端全部 7 个模块
│   │   ├── overview.js              # 老师端总览统计
│   │   ├── students.js              # 学生 CRUD + 筛选 + CSV导出 + 详情弹窗
│   │   ├── classes.js               # 班级 CRUD
│   │   ├── schedule.js              # 周课表渲染
│   │   ├── attendance.js            # 每日点名
│   │   ├── attendance-stats.js      # 出勤统计 + 历史
│   │   ├── records.js               # 上课记录
│   │   ├── lesson-log.js            # 课消日志 + 手动调整 + 低课次告警
│   │   ├── artworks.js              # 作品管理（图片压缩）
│   │   ├── announcements.js         # 通知发布
│   │   ├── inquiries.js             # 预约查询
│   │   └── courses.js               # 课程编辑
│   ├── login.js                     # 登录/注册/忘记密码逻辑
│   ├── contact.js                   # 预约表单逻辑
│   └── gallery.js                   # 作品页渲染
├── functions/
│   ├── dbProxy/                     # 数据库代理云函数
│   │   └── index.js
│   └── verify-code/                 # 邮箱验证码云函数
│       ├── index.js
│       └── package.json
├── images/                          # 图片资源
├── sitemap.xml                      # 搜索引擎站点地图
├── robots.txt                       # 爬虫访问规则
└── 开发文档.md                       # 本文档
```

---

## 页面地图

```
公开页面（无需登录）
├── index.html                 → 首页（画室简介+课程卡片+作品+联系方式）
├── gallery.html               → 作品展示（美术+书法+课堂剪影）
├── courses.html               → 课程介绍（6门课详情+时间表+报名流程）
├── contact.html               → 联系/预约（表单+微信通知+交通指引）
└── login.html                 → 登录/注册/忘记密码

登录后（受保护）
├── teacher-dashboard.html     → 老师管理端
└── parent-dashboard.html      → 家长端
```

---

## 功能总览

### 对外展示
- 首页：画室简介、画室环境照片、联系信息（4×1网格 + 微信二维码）
- 画廊：美术/书法/课堂剪影三区展示 + 图片灯箱
- 课程：6门课（美术: 儿童创意画/中国画/素描/色彩；书法: 硬笔/软笔）+ 课程体系表 + 报名流程 + 课程配图
- 预约：表单提交 → localStorage + CloudBase + 微信通知 + 交通指引

### 登录系统
- **家长登录**：邮箱+密码，本地密码哈希验证（查 `parents` 集合，不依赖 CloudBase 认证 API）
- **家长注册**：6 字段（姓名+邮箱+验证码+**密码**+手机号+孩子名），验证码通过 QQ SMTP 发送
- **教师登录**：邮箱+密码（2 个预置账号硬编码），仅教师标签页显示，隐藏注册入口
- **忘记密码**：家长通过 CloudBase 邮件重置；教师需联系管理员
- **密码可见切换**：SVG 睁眼/闭眼图标（替代原 👁️🙈 表情符号）
- **删除记住我**：始终使用 localStorage 持久化登录态
- **验证码云函数**：部署为 HTTP Web 函数（`--httpFn`），自带 CORS 处理
- **实时校验** / **按钮 loading 防重复**

### 家长端（7 模块 + 多孩子子系统）
| 模块 | 内容 |
|------|------|
| 📊 总览 | 下次上课 / 剩余课次 / 出勤率 / 作品数 + 最近作品 + 最新通知 |
| 📅 我的课程 | 周课表（从教师排课数据）+ 班级详情卡片 |
| 📝 上课记录 | 出勤统计 + 考勤明细（出勤/请假/缺勤/扣课次） |
| 📉 课次明细 | 课次进度条 + 消课日志（合并点名+手动调整） |
| 🖼️ 孩子作品 | 按当前孩子筛选展示（支持 studentId + name 双匹配） |
| 📢 画室通知 | 教师发布的通知列表 |
| ⚙️ 个人信息 | 家长/孩子信息 + 修改密码 + **孩子管理（添加/切换/删除）** |
| 👶 孩子管理 | 输入姓名搜索匹配 → 发送关联请求 → 教师端审批通过后关联 |
| 🔄 顶栏切换 | 多孩子时顶栏下拉快速切换，各模块数据跟随刷新 |

### 教师端（7 大模块 + 6 子标签）
| 模块 | 内容 |
|------|------|
| 📊 总览 | 4 统计卡片 + 今日课程表 |
| 👨‍🎓 学生管理 | 学员(CRUD+搜索筛选+CSV导出+详情弹窗) / 班级 / 课表 / 点名(出勤+扣课次+统计) / 上课记录 |
| 📉 课消日志 | 消课明细 + 手动调整 + 低课次预警 |
| 🖼️ 作品管理 | 完整CRUD + CloudBase云存储 + 筛选搜索 + 卡片/表格双视图 + 批量操作 + CSV导出 + 图片灯箱 + 手机相册上传 |
| 📩 预约查询 | 已读/未读 + 30s 自动刷新 + 一键清空 |
| 📢 发布通知 | 发布 + 删除 |
| 📚 课程管理 | 6 门默认课程行内编辑 |

### 全局功能
- 🌓 深色模式 | 🔍 图片灯箱 | ⬆️ 回到顶部 | 📱 响应式（手机/平板）| ☁️ 同步状态气泡

### 作品管理（2026-06-18 重做）
- **完整 CRUD**: 添加 / 编辑 / 删除（单个+批量），编辑保留 lastModifiedAt/lastModifiedBy
- **CloudBase 云存储**: 图片上传 `cloud://` fileID，批量解析临时URL，删除时清理云文件
- **图片压缩**: 800px/JPEG 75%，竞态保护（压缩中禁用保存按钮）
- **筛选搜索**: 作品名/学生名 + 类型(美术/书法/课堂剪影/家长端) + 学生 + 3种排序
- **双视图**: 卡片网格 ↔ 表格列表切换
- **批量操作**: 勾选多件 → 批量删除
- **CSV 导出**: 按当前筛选结果导出
- **手机端**: 支持拍照 + 相册选择（已移除 `capture` 属性）
- **数据模型**: id/title/student/studentId/type/image/addedAt/addedBy/lastModifiedAt/lastModifiedBy
- **公开画廊**: 按 type 分流（课堂剪影置顶 → 美术 → 书法），`家长端` 类型不出现在公开页

---

## 数据架构

### 存储策略
```
localStorage（主） → 读写即时
CloudBase（备份）  → Web SDK 直接数据库访问（匿名登录）

同步机制（2026-06-18 更新）：
- 同步通道: CloudBase Web SDK 直接数据库访问（不经过云函数）
- 依赖安全规则: 业务集合开放 read:true, write:"auth != null"
- dbProxy 云函数保留作为备用通道（当前不使用）
- parents / email_codes 保持仅 owner 可读写（保护隐私）
```

### 数据集合（10 个）
| 集合 | 用途 |
|------|------|
| `students` | 学生档案 |
| `classes` | 班级排课 |
| `attendance` | 考勤记录 |
| `records` | 上课记录 |
| `corrections` | 手动课次调整 |
| `artworks` | 学生作品 |
| `announcements` | 画室通知 |
| `inquiries` | 试听预约 |
| `parents` | 家长账号 |
| `email_codes` | 邮箱验证码（5 分钟 TTL） |

### 云函数
| 函数 | 用途 | 调用方式 | 权限 |
|------|------|----------|------|
| `dbProxy` | 数据库代理（管理端读写） | SDK `callFunction` | 管理员 |
| `verify-code` | 发送 + 校验邮箱验证码 | **HTTP 直连** (fetch) | 公开 |

---

## 认证体系

### 教师账号
在 `js/app/auth.js` → `AUTH_CONFIG.teachers` 硬编码：
| 姓名 | 邮箱 | 角色 |
|------|------|------|
| 张校长 | 756924037@qq.com | admin |
| 郑校长 | 953034984@qq.com | teacher |

### 家长认证
- **邮箱密码**：CloudBase 邮箱注册/登录
- **邮箱验证码**：云函数发邮件 → 验证 → 创建账号 → 自动登录
- 注册字段：姓名、邮箱、验证码、手机号（选填）、孩子姓名
- 新用户注册后自动创建会话，直接进入家长端

### 权限
```js
hasAdminPermission() → user.role === 'admin' || user.role === 'teacher'
```
- admin：张校长，可删除任何记录、CSV 导出、清空预约
- teacher：郑校长，日常操作权限
- 权限覆盖：学生删除、班级删除、记录删除、手动消课等

### 会话管理
- `记住我` 勾选 → localStorage（7 天持久化）
- 不勾选 → sessionStorage（关闭浏览器自动清除）

---

## 开发阶段

| 阶段 | 内容 | 状态 |
|------|------|------|
| 1 | HTML 首页框架 | ✅ |
| 2 | CSS 视觉设计（暖色调+卡片） | ✅ |
| 3 | 多页面（画廊+课程+联系） | ✅ |
| 4 | JS 交互（灯箱+深色+回顶） | ✅ |
| 5 | 登录系统（CloudBase + 邮箱验证码） | ✅ |
| 6 | 部署上线（GitHub Pages） | ✅ |
| 7 | 教师端全部功能 | ✅ |
| 8 | 家长端 7 模块 | ✅ |
| 9 | 登录优化（密码可见/记住我/实时校验等） | ✅ |
| 10 | 对外展示优化（SEO/CSS架构/性能/结构化数据） | ✅ |
| 11 | 课程体系重构 + 课程配图 + 首页照片 + 作品管理修复 | ✅ |
| 12 | 教师端继续填充 | ⏳ 待做 |

---

## 关键配置位置

| 配置项 | 文件 |
|------|------|
| 教师账号 | `js/app/auth.js` → `AUTH_CONFIG.teachers` |
| 微信通知 Key | `js/app/auth.js` → `AUTH_CONFIG.notifyKeys` |
| CloudBase 环境 ID | `js/app/config.js` → `CLOUDBASE_CONFIG.env` |
| 默认课程 | `js/dashboard/courses.js` → `DEFAULT_COURSES` |
| 云函数配置 | `cloudbaserc.json` |
| QQ 邮箱 SMTP | CloudBase 控制台 → verify-code 环境变量 `EMAIL_USER` / `EMAIL_PASS` |

---

## 日常操作

### 部署到线上
```bash
git add -A && git commit -m "描述改了什么" && git push
# GitHub Pages 自动部署，等 1 分钟生效
```

### 本地测试
```bash
cd "G:/春晓画室"
python -m http.server 8899
# 浏览器打开 http://localhost:8899/login.html
```
> ⚠️ 不能直接双击打开 HTML 文件！浏览器会拦截 `file://` 到 `https://` 的请求。

### 添加新老师
编辑 `js/app/auth.js`，在 `teachers` 数组添加：
```js
{ email: '新老师@qq.com', password: '密码', name: '某老师', role: 'teacher' }
```
推送即可。

### 验证码邮件配置

**架构（2026-06-17 更新）：**
- 前端不再通过 SDK `callFunction` 调用（浏览器存在兼容问题），改为 `fetch()` HTTP 直连
- CloudBase HTTP 访问服务 → Event 函数，公开 URL 无需登录态
- 前端 `js/login.js` → `callVerifyCode()` 函数封装

**首次配置：**
1. QQ 邮箱 → 设置 → 账户 → POP3/SMTP → 开启 → 生成授权码
2. CloudBase 控制台 → 云函数 verify-code → 环境变量：
   - `EMAIL_USER` = QQ 邮箱地址
   - `EMAIL_PASS` = 授权码（非 QQ 密码）
3. `email_codes` 集合由云函数自动创建（不再需要手动建表）

---

### 验证码故障排查
```bash
# 1. 检查云函数环境变量
npx tcb fn invoke verify-code --params '{"action":"ping"}'
# → 应返回 hasEmailUser:true, hasEmailPass:true

# 2. 查看最近调用日志
npx tcb fn log verify-code --limit 20

# 3. 测试发送（CLI 调用，绕过前端）
npx tcb fn invoke verify-code --params '{"action":"send","email":"你的邮箱@qq.com"}'

# 4. 常见错误：
# "邮箱授权码无效" → QQ 授权码过期，去 QQ 邮箱重新生成
# "邮件服务器连接失败" → SMTP 网络不通，稍后重试
# "发送过于频繁" → 触发限流，等一会再试
# "email_codes collection not exist" → 云函数自动创建失败
```

---

## 待办

| 优先级 | 事项 |
|------|------|
| ~~🔴~~ | ~~确认 email_codes 集合已创建~~ ✅ |
| ~~🔴~~ | ~~确认 verify-code 环境变量正确~~ ✅ |
| ~~🟡~~ | ~~替换占位图片（placehold.co）~~ ✅ CSS 渐变占位替代 |
| ~~🟡~~ | ~~对外展示页面优化~~ ✅ SEO + CSS架构 + 性能 |
| 🟢 | 教师端继续填充 |
| 🟢 | 家长端课程表支持"今日"展示实际上课内容 |

---

## 2026-06-17 更新日志

### 对外展示页面全面优化（`f91ec59`）
- **SEO**：4 页面添加 `<meta description>` + Open Graph 标签（微信/QQ 分享预览）
- **性能**：9 个 placehold.co → CSS 渐变占位；`loading="lazy"` + `width/height`；`<link preconnect>`；`<script defer>`
- **CSS 架构**：自定义属性（设计令牌）+ 平板断点 768px + `@media print` + `prefers-reduced-motion`
- **courses.html**：40+ 处内联 `style=""` 提取为 CSS 类
- **结构化数据**：index.html 嵌入 `Schema.org EducationalOrganization`
- **新建**：`sitemap.xml` + `robots.txt`

### 数据层双向同步修复（`c94e43f`→`5487d58`）
- **根因**：`pullFromCloud` 只在 localStorage 为空时拉取，之后永远跳过
- **修复**：
  - 每次加载比较 `_synced` 与云端 `updatedAt` 时间戳
  - 云端更新 → 覆盖本地 / 本地更新 → 推送云端
  - 旧数据迁移保护：有数据但无 `_synced` → 本地优先
  - `save*()` 立即标记 `_synced`，推送失败后下次自动重试
  - 家长端补全同步流程（之前完全没有）
- **权限**：全部改用 `dbProxy` 云函数（管理员权限，绕过安全规则）

### CSS 冲突修复（`64e80a7`）
- `.schedule-table` 类名冲突 → 公开版改名 `.course-schedule-table`
- `nav a` 样式渗透到教师端侧边栏 → 限定 `header nav a`
- dashboard.css 显式重置 `h2` / `h3` 样式

---

## 2026-06-18~19 更新日志

### 课程体系重构（`037d137`）
- **课程变更**: 新增中国画(8岁)/色彩(10岁)，移除水彩入门/动漫插画
- 年龄调整: 儿童创意画4+、素描10+（原8-12）、软笔6+（原8+）
- 全部课程统一每周1次、每次120分钟
- 时段改为"咨询画室安排"

### 课程页面重布局（`5866d1e`）
- 课程体系表移至顶部（原"课程时间总览"改名）
- 删除原页面标题+介绍段落
- 6门课程卡片改为文字左+图片右布局（`.course-detail-vertical` flex布局）
- 每门课程右侧配课程图片（`images/` 下6张JPG）
- 表底新增"预约免费试听课"气泡链接 → contact.html

### 首页画室照片
- 关于画室区域新增照片网格（`.about-gallery`）
- 已添加 `studio-1.jpg`，待补 studio-2.jpg / studio-3.jpg（1200×800）

### 作品管理修复（`a5fe4aa`）
- 移除 file input 的 `capture="environment"` 属性
- 手机端上传支持拍照 + 相册选择

### 学生多课程报名系统（`5b3c2bf`）
- Student 数据模型新增 `enrollments: [{course, totalLessons, consumedLessons}]` 数组
- 每门课独立追踪课次，顶层字段自动计算兼容旧数据
- 学生表单改为动态 enrollment 行（支持 1-6 门课，+添加/✕删除）
- 点名扣课次按班级课程自动匹配 enrollment（`cls.course → enrollment.course`）
- 详情弹窗展示分课程进度条
- 课消日志手动调整增加课程选择器
- 低课次预警改为按 enrollment 粒度检查
- 旧数据无 enrollments 时自动迁移（`normalizeStudentEnrollments()`）

### 全局术语重命名（`5b3c2bf`）
- 13 个文件中「课时」→「课次」全部替换（HTML/CSS/JS/文档，71 处）

### 点名优化（`5eaa7d7` / `f78168c` / `61f05ba`）
- 点名扣课次默认值改为 1（原 0），切换出勤/请假/缺勤自动设 1/0
- 课消日志点名保存后自动刷新
- 点名保存后保持用户选择的日期，不跳回今天
- 班级学员匹配改用宽松比较（`==`），兼容数字/字符串 ID 类型差异
- 匹配失败时显示诊断信息（班级 studentIds + 学生总数）

### 老师端顶栏导航修正（`b34b9d3`）
- 教师端顶部导航「📞 联系我们」→「🏠 回到首页」（链接 contact.html → index.html）
- 教师已登录后不需要预约入口，回到首页更合理

### 登录页重做（`7534c16` → `ee277d8`）
- **注册加密码字段**：家长注册时设置密码，存入 `parents.passwordHash`
- **SVG 密码图标**：睁眼/闭眼 SVG 替代 👁️🙈 表情符号
- **教师端隐藏注册**：教师标签页不显示「点击注册」入口
- **删除记住我**：移除复选框及相关逻辑，统一用 localStorage
- **验证码 CORS 修复**：verify-code 部署为 HTTP Web 函数（`--httpFn`），自带 CORS

### 家长本地密码认证（`dfcfd12`）
- CloudBase Web SDK 无 `signUpWithEmailAndPassword`，改为本地密码哈希
- 注册时 `Auth.hashPassword()` 存入 `parents.passwordHash`
- 登录时查 `parents` 集合 + `Auth.verifyPassword()` 比对
- 修改密码直接更新 `parents.passwordHash`
- `parents` 集合安全规则 read 改为 `true`（支持登录查询）

### 家长端多孩子子系统（`226c092`）
- `parents` 集合新增 `children: [{name, studentId}]` + `activeChildIndex`
- 会话存储 children 数组，`Auth.currentChild()` 获取当前孩子
- 个人信息页新增「我的孩子」区块：孩子列表 + 添加/设为当前/删除
- 顶栏孩子切换器：下拉快速切换，各模块数据跟随刷新
- `findChildStudent()` 改为按 `children[activeChildIndex]` 查找
- `Auth.updateChildren()` 同步更新 session + CloudBase
- 旧 `childName` 格式登录时自动迁移为 `children[]`
- 学生表新增 `parentEmail` 可选字段

### 孩子关联审批模式（`f171e45`）
- 添加孩子改为输入姓名搜索（去掉下拉框）
- 精确匹配学生姓名 → 创建 `type: link_request` 存入 inquiries
- 注册时自动匹配学生姓名，匹配即关联
- 教师端预约查询页显示关联请求卡片（✅通过 / ❌拒绝）
- `approveLinkRequest()`：更新学生 parentEmail + 家长 children 数组
- `rejectLinkRequest()`：标记为已拒绝

---

## 开发原则（2026-06-17 建立）

> **改任何文件前，先确认它对整个架构的影响。**

- `css/style.css` 被 7 个页面加载（公开4页 + login + teacher + parent）
- `js/app/data.js` 被 5 个页面加载（index + gallery + teacher + parent + contact 间接）
- 新增 CSS 类名前用 Grep 检查是否与 `dashboard.css` 冲突
- 修改 JS 函数前用 Grep 检查所有调用方
- 公开页专属样式应限定作用域（`header nav`、`.course-*` 等前缀）
